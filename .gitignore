# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# secrets / local env
.env
.env.local
.env.*.local

# logs & coverage
*.log
coverage/

# analytics store (runtime data — regenerated by the server)
data/
*.analytics.json

# OS
.DS_Store
Thumbs.db

# packaged build artifacts (regenerate with `npm run pack`).
# Distribute these via GitHub Releases instead of committing them.
# Remove these two lines if you do want them tracked in the repo.
bundle/
*.mcpb

# scratch / verification harnesses
.tcheck*/

# stray/accidental empty files from shell redirects
/27372

# internal working documents (kept locally, not published in the public repo)
/GIT-SETUP.md
/specs/
/docs/agent-page-content.md
/docs/MCP-Endpoints-Handoff-*.md
/docs/MCP-Server-Audit-*.md

# mcp-publisher CLI (download locally, don't commit)
mcp-publisher
mcp-publisher.exe
mcp-publisher*.tar.gz
